// const gridContainer = document.querySelector('.steps__cards');
// const gridItemCount = gridContainer.children.length;

// if (gridItemCount >= 5) {
//     gridContainer.style.gridTemplateColumns = 'repeat(auto-fit, minmax(250px, 1fr))';
// } else {
//     gridContainer.style.gridTemplateColumns = 'repeat(auto-fit, 280px)';
// }